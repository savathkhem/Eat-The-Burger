<li>
	{{name}}

	<button class="change-sleep" data-id="{{id}}" data-newsleep="{{sleep}}">
		{{#if sleep}}Eat It!{{else}}Make Another One!{{/if}}
	</button>

	<button class="delete-cat" data-id="{{this.id}}">Trash It!</button>
</li>
